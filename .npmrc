# npmmirror (and some other registries) do not implement npm's security audit API.
# npm 11 treats that 404 as a hard install error. Skip the install-time audit.
# To check advisories: npm audit --registry=https://registry.npmjs.org/
audit=false
