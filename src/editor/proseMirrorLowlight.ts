import { createLowlight, common } from 'lowlight'

/** TipTap CodeBlockLowlight uses: a subset of commonly used languages to reduce packaging size*/
export const proseMirrorLowlight = createLowlight(common)
