import type { MenuBarStructureGroup, MenuTreeNode, ToolbarLayout } from './commandManifest.types'
import { LUNA_TEXT_COLOR_PRESETS } from '../editor/lunaTextColor'

const S = (): MenuTreeNode => ({ kind: 'separator' })
const C = (id: string): MenuTreeNode => ({ kind: 'command', id })
const sub = (id: string, labelKey: string, children: MenuTreeNode[]): MenuTreeNode => ({
  kind: 'submenu',
  id,
  labelKey,
  children,
})

const fmtImageZoom: MenuTreeNode[] = [
  C('fmt-image-zoom-25'),
  C('fmt-image-zoom-33'),
  C('fmt-image-zoom-50'),
  C('fmt-image-zoom-60'),
  C('fmt-image-zoom-80'),
  S(),
  C('fmt-image-zoom-100'),
  S(),
  C('fmt-image-zoom-120'),
  C('fmt-image-zoom-150'),
]

const fmtTextColor: MenuTreeNode[] = [
  ...LUNA_TEXT_COLOR_PRESETS.map((preset) => C(`fmt-text-color-${preset.id}`)),
  S(),
  C('fmt-text-color-default'),
]

/** Top bar menu tree: only command id + submenu structure, no label/icon/shortcut*/
export const MENU_BAR_STRUCTURE: readonly MenuBarStructureGroup[] = [
  {
    id: 'bar-file',
    labelKey: 'menu.file',
    children: [
      C('file-new'),
      C('file-new-tab'),
      C('file-new-window'),
      C('daily-note-open'),
      C('daily-note-open-yesterday'),
      C('daily-note-open-tomorrow'),
      S(),
      sub('sub-templates', 'menu.file.templates', [
        C('file-new-from-template'),
        S(),
        C('template-edit-default'),
        C('template-edit-daily'),
        C('template-open-folder'),
        S(),
        C('template-preferences'),
      ]),
      S(),
      sub('sub-open', 'menu.file.open', [C('file-open-file'), C('open-folder')]),
      C('file-recent-placeholder'),
      S(),
      C('file-close-workspace'),
      S(),
      C('file-show-intro'),
      C('file-reveal'),
      S(),
      C('file-delete'),
      S(),
      C('file-close'),
      S(),
      C('save'),
      C('file-save-as'),
      C('file-copy-path'),
      C('file-rename'),
      S(),
      sub('sub-file-history', 'menu.file.history', [
        C('file-history-open'),
        C('file-history-create-snapshot'),
        S(),
        C('file-revert'),
      ]),
      C('file-save-all'),
      S(),
      C('file-import'),
      sub('sub-export', 'menu.file.export', [
        C('file-export-pdf'),
        C('file-export-markdown'),
        C('file-export-html'),
        C('file-export-html-plain'),
        C('file-export-image'),
        S(),
        C('file-export-word'),
      ]),
      S(),
      C('preferences'),
      C('file-print'),
    ],
  },
  {
    id: 'bar-edit',
    labelKey: 'menu.edit',
    children: [
      C('edit-undo'),
      C('edit-redo'),
      S(),
      C('edit-cut'),
      C('edit-copy'),
      C('edit-paste'),
      S(),
      C('edit-copy-plain'),
      C('edit-copy-md'),
      C('edit-copy-html'),
      S(),
      C('edit-paste-plain'),
      S(),
      sub('sub-edit-select', 'menu.edit.select', [C('edit-select-all'), C('edit-select-block')]),
      S(),
      C('edit-delete'),
      sub('sub-edit-del-range', 'menu.edit.deleteRange', [C('edit-delete-block'), C('edit-delete-line')]),
      S(),
      sub('sub-edit-find', 'menu.edit.find', [
        C('edit-find'),
        C('edit-find-prev'),
        C('edit-find-next'),
        S(),
        C('edit-find-replace'),
        C('edit-replace-next'),
        S(),
        C('edit-jump-to-selection'),
      ]),
      S(),
      C('edit-emoji'),
      S(),
      sub('sub-edit-eol', 'menu.edit.eol', [C('edit-eol-crlf'), C('edit-eol-lf')]),
      C('edit-indent-first-line'),
      S(),
      C('edit-show-br'),
    ],
  },
  {
    id: 'bar-paragraph',
    labelKey: 'menu.paragraph',
    children: [
      C('para-h1'),
      C('para-h2'),
      C('para-h3'),
      C('para-h4'),
      C('para-h5'),
      C('para-h6'),
      S(),
      C('para-paragraph'),
      S(),
      C('para-heading-up'),
      C('para-heading-down'),
      S(),
      sub('sub-para-table', 'menu.para.table', [
        C('para-table-insert'),
        S(),
        C('para-table-row-above'),
        C('para-table-row-below'),
      ]),
      C('para-math-block'),
      C('para-insert-code-block'),
      sub('sub-para-code-tools', 'menu.para.codeTools', [
        C('para-code-copy'),
        C('para-code-tools-indent-selection'),
        C('para-code-tools-indent-block'),
      ]),
      sub('sub-para-callout', 'menu.para.callout', [
        C('para-callout-tip'),
        C('para-callout-suggestion'),
        C('para-callout-important'),
        C('para-callout-warning'),
        C('para-callout-caution'),
      ]),
      S(),
      C('para-quote'),
      S(),
      C('para-ol'),
      C('para-ul'),
      C('para-task'),
      sub('sub-para-task', 'menu.para.taskState', [C('para-task-done'), C('para-task-undone')]),
      sub('sub-para-list-indent', 'menu.para.listIndent', [
        C('para-list-indent-more'),
        C('para-list-indent-less'),
      ]),
      S(),
      C('para-insert-paragraph-above'),
      C('para-insert-paragraph-below'),
      S(),
      C('para-link-ref'),
      C('para-footnote'),
      S(),
      C('para-hr'),
      C('para-toc'),
    ],
  },
  {
    id: 'bar-format',
    labelKey: 'menu.format',
    children: [
      C('fmt-bold'),
      C('fmt-italic'),
      C('fmt-underline'),
      C('fmt-inline-code'),
      S(),
      C('fmt-inline-math'),
      C('fmt-strike'),
      C('fmt-highlight'),
      C('fmt-comment'),
      S(),
      C('fmt-link'),
      sub('sub-fmt-link-ops', 'menu.fmt.linkOps', [C('fmt-link-open'), C('fmt-link-copy')]),
      sub('sub-fmt-image', 'menu.fmt.image', [
        C('fmt-image'),
        C('fmt-image-insert-local'),
        S(),
        C('fmt-image-reveal'),
        C('fmt-image-rename'),
        sub('sub-fmt-image-zoom', 'menu.fmt.image.scale', fmtImageZoom),
        sub('sub-fmt-image-conv', 'menu.fmt.image.conv', [C('fmt-image-as-html'), C('fmt-image-as-md')]),
        S(),
        C('fmt-image-delete'),
        S(),
        C('fmt-image-copy-to'),
        C('fmt-image-upload'),
        S(),
        C('fmt-image-copy-all'),
        C('fmt-image-move-all'),
        C('fmt-image-upload-all-local'),
        S(),
        C('fmt-image-reload-all'),
        S(),
        sub('sub-fmt-image-on-insert', 'menu.fmt.image.onInsert', [
          C('fmt-image-on-insert-copy'),
          C('fmt-image-on-insert-upload'),
        ]),
        C('fmt-image-root-dir'),
        C('fmt-image-global-settings'),
      ]),
      S(),
      sub('sub-fmt-text-color', 'ctx.editor.textColorSubmenu', fmtTextColor),
      S(),
      C('fmt-clear-style'),
    ],
  },
  {
    id: 'bar-view',
    labelKey: 'menu.view',
    children: [
      C('toggle-source-mode'),
      S(),
      C('toggle-focus'),
      S(),
      C('toggle-sidebar'),
      C('view-sidebar-outline'),
      C('view-sidebar-files'),
      C('view-search'),
      S(),
      C('view-word-count'),
      S(),
      C('view-zoom-in'),
      C('view-zoom-out'),
      S(),
      C('view-fullscreen'),
      C('view-live-preview'),
    ],
  },
  {
    id: 'bar-window',
    labelKey: 'menu.window',
    children: [
      C('win-minimize'),
      C('win-zoom'),
      S(),
      sub('sub-win-move', 'menu.win.move', [
        sub('sub-win-half', 'menu.win.half', [
          C('win-move-resize-half-left'),
          C('win-move-resize-half-right'),
          C('win-move-resize-half-top'),
          C('win-move-resize-half-bottom'),
        ]),
      ]),
      C('win-tile-full'),
    ],
  },
  {
    id: 'bar-help',
    labelKey: 'menu.native.help',
    children: [
      C('help-shortcuts'),
      S(),
      C('help-about'),
      S(),
      C('help-privacy'),
      C('help-website'),
      C('help-feedback'),
    ],
  },
]

/** Toolbar dropdown trigger id → child manifest command ids (children are not ui.toolbar).*/
export const TOOLBAR_DROPDOWN_CHILDREN: Partial<Record<string, readonly string[]>> = {
  'toolbar-callout': [
    'para-callout-tip',
    'para-callout-suggestion',
    'para-callout-important',
    'para-callout-warning',
    'para-callout-caution',
  ],
}

/** toolbar slot → command id list*/
export const TOOLBAR_LAYOUT: ToolbarLayout = {
  'sidebar-header': ['open-folder', 'view-sidebar-outline'],
  'editor-format': [
    'fmt-bold',
    'fmt-italic',
    'fmt-underline',
    'fmt-inline-code',
    'fmt-strike',
    'fmt-highlight',
    'fmt-link',
    'toolbar-callout',
  ],
}
